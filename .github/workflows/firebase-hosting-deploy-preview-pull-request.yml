# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
on: pull_request
permissions:
  checks: write
  contents: read
  pull-requests: write
jobs:
  build_and_preview:
    if: ${{ github.event.pull_request.head.repo.full_name == github.repository }}
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20.x' # Upgrade Node.js to a compatible version for firebase CLI    
        
      - name: Install Firebase CLI
        run: npm install Firebase-tools@14.10.1

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.410'

      # #
      # # OrchestratorService
      # #
      # - name: Build and publish Orchestrator Service
      #   run: | 
      #     chmod +x ./scripts/publish-services.ps1
      #     ./scripts/publish-services.ps1 OrchestratorService     

      # #
      # # AgentService
      # #
      # - name: Build and publish AgentService Service
      #   run: | 
      #     chmod +x ./scripts/publish-services.ps1
      #     ./scripts/publish-services.ps1 AgentService     

      # # deploy services to cloud run using GH actions
      # - name: Deploy to Cloud Run (OrchestrastorService)      
      #   uses: 'google-github-actions/deploy-cloudrun@v2'
      #   with:
      #     service: 'orchestratorservice'
      #     source: ./OrchestratorService

      # # deploy services to cloud run using GH actions
      # - name: Deploy to Cloud Run (AgentService)      
      #   uses: 'google-github-actions/deploy-cloudrun@v2'
      #   with:
      #     service: 'AgentService'
      #     source: ./AgentService

      # - name: 'Use output'
      #   run: 'curl "${{ steps.deploy.outputs.url }}"'

      #
      # OrchestratorWebApp
      #
      - name: Build and publish Blazor app
        run: |
          chmod +x ./scripts/publish-orchestratorwebapp.ps1
          ./scripts/publish-orchestratorwebapp.ps1

      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_AGENT_AS_A_SERVICE_459620 }}
          projectId: agent-as-a-service-459620
  